version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: checa-ai-postgres
    environment:
      POSTGRES_DB: checa_ai
      POSTGRES_USER: checa_ai_user
      POSTGRES_PASSWORD: dev_password123
      PGDATA: /data/postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/data/postgres
    restart: unless-stopped
    networks:
      - checa_ai_network

  pgadmin:
    image: dpage/pgadmin4
    container_name: checa-ai-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@checa-ai.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "8080:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    restart: unless-stopped
    networks:
      - checa_ai_network
    depends_on:
      - postgres

volumes:
  postgres_data:
  pgadmin_data:

networks:
  checa_ai_network:
    driver: bridge